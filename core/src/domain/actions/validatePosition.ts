import { PlayerFacade } from "../playerFacade";
import { Position } from "../position";

export function ValidatePosition(player: PlayerFacade, verifiedPosition: Position) {
    // x(player.view.x, player.view.y, verifiedPosition)
}