{
  "name": "multiplayer-browser-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install --only=dev && npm install && npm run build && cd .. && echo 'PRINT LS:' && ls"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fedejure/multiplayer-game-core": "^1.0.0",
    "@geckos.io/phaser-on-nodejs": "1.2.4",
    "body-parser": "^1.19.0",
    "express": "4.17.1",
    "phaser": "3.55.2",
    "socket.io": "4.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "parallel": "^1.2.0",
    "typescript": "4.3.5"
  }
}
