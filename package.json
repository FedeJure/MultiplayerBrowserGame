{
  "name": "multiplayer-browser-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "NODE_ENV=production && npm install && cd app && npm install && npm run build",
    "build:dev": "npm link @fedejure/multiplayer-game-core && cd app && npm run build && cd ..",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fedejure/multiplayer-game-core": "1.1.1",
    "@geckos.io/phaser-on-nodejs": "1.2.7",
    "body-parser": "1.19.0",
    "express": "4.17.1",
    "object-sizeof": "^1.6.3",
    "phaser": "3.55.2",
    "prom-client": "^14.0.1",
    "socket.io": "^4.4.0"
  },
  "devDependencies": {
    "@types/socket.io": "^3.0.2",
    "nodemon": "^2.0.12",
    "parallel": "^1.2.0",
    "typescript": "4.3.5"
  }
}
